export interface Theme {
  colors: {
    primary: string;
    secondary: string;
    accent: string;
  };
}

export const theme: Theme = {
  colors: {
    primary: 'brown',
    secondary: 'white',
    accent: 'red'
  }
};
